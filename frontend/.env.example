# Environment variables example
# For local development, copy this to .env.local and modify as needed
# VITE_API_URL=http://localhost:8000

# For production/test builds, .env.production will be used automatically
# which uses relative URLs (same domain as frontend)
