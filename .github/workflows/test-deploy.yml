name: Test Branch CI/CD

on:
  push:
    branches:
      - test
  pull_request:
    branches:
      - test

env:
  PYTHON_VERSION: '3.11'
  NODE_VERSION: '18'

jobs:
  # Job 1: Run Tests
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'
        
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json
        
    - name: Install Python dependencies
      run: |
        cd scraper
        pip install -r requirements.txt
        cd ../backend
        pip install -r requirements.txt
        
    - name: Install frontend dependencies
      run: |
        cd frontend
        npm ci --ignore-scripts --ignore-scripts
        
    - name: Test scraper
      run: |
        cd scraper
        python3 -c "
import sys
# Don't try to connect to database during import test
import os
os.environ['DATABASE_URL'] = 'postgresql://dummy:dummy@dummy:5432/dummy'
import scraper_enhanced
print('✓ Scraper module loaded successfully')
"
        
    - name: Test backend
      run: |
        cd backend
        python3 -c "import main_enhanced; print('Backend module loaded successfully')"
        
    - name: Build frontend
      run: |
        cd frontend
        npm run build
        
    - name: Test frontend build
      run: |
        cd frontend
        test -d dist && echo "Frontend build successful"
        
  # Job 2: Deploy to Test Server via Cloudflare Tunnel
  deploy:
    name: Deploy to Test Server
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/test'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: 💾 Cache cloudflared CLI
      id: cache-cloudflared
      uses: actions/cache@v3
      with:
        path: /usr/local/bin/cloudflared
        key: cloudflared-${{ runner.os }}-latest
        
    - name: 🛠️ Install cloudflared CLI
      if: steps.cache-cloudflared.outputs.cache-hit != 'true'
      run: |
        curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
        sudo dpkg -i cloudflared.deb
        rm cloudflared.deb
        
    - name: 🔑 Authenticate with Cloudflare Tunnel
      env:
        CF_TUNNEL_ID: ${{ secrets.CF_TUNNEL_ID }}
      run: |
        # Verify cloudflared is working
        /usr/local/bin/cloudflared --version
        
        echo "Using Cloudflare Tunnel ID: ${CF_TUNNEL_ID}"
        
    - name: 🔧 Configure SSH for Cloudflare Tunnel
      env:
        SSH_USER: ${{ secrets.SSH_USER }}
        CF_TUNNEL_ID: ${{ secrets.CF_TUNNEL_ID }}
        CF_SSH_HOSTNAME: ${{ secrets.CF_SSH_HOSTNAME }}
      run: |
        # Configure SSH to use Cloudflare Tunnel
        mkdir -p ~/.ssh
        chmod 700 ~/.ssh
        
        # Use cloudflared to proxy SSH through the tunnel
        # The tunnel connects to your workstation's SSH service
        cat > ~/.ssh/config << EOF
        Host cf-tunnel
            Hostname ${CF_SSH_HOSTNAME}
            User ${SSH_USER}
            ProxyCommand /usr/local/bin/cloudflared access tcp --hostname ${CF_SSH_HOSTNAME} --url tcp://localhost:7022
            StrictHostKeyChecking no
            UserKnownHostsFile=/dev/null
        EOF
        
        chmod 600 ~/.ssh/config
        
        echo "SSH config created:"
        cat ~/.ssh/config
        echo ""
        echo "Testing cloudflared connection..."
        /usr/local/bin/cloudflared --version
        
    - name: 🚀 Deploy Backend via Cloudflare Tunnel
      env:
        DEPLOY_PATH: ${{ secrets.TEST_DEPLOY_PATH }}
      run: |
        ssh cf-tunnel << 'ENDSSH'
          set -e
          cd '${{ secrets.TEST_DEPLOY_PATH }}'
          
          echo "📥 Pulling latest code..."
          git fetch origin
          git checkout test
          git pull origin test
          
          echo "🔧 Updating backend..."
          cd backend
          pip3 install -r requirements.txt --quiet
          
          echo "♻️ Restarting backend service..."
          sudo systemctl restart aaip-backend-test || echo "⚠️ Manual restart required"
          
          echo "✅ Backend deployment complete"
        ENDSSH
        
    - name: 🎨 Deploy Frontend via Cloudflare Tunnel
      run: |
        ssh cf-tunnel << 'ENDSSH'
          set -e
          cd '${{ secrets.TEST_DEPLOY_PATH }}/frontend'
          
          echo "📦 Installing frontend dependencies..."
          npm ci --quiet
          
          echo "🏗️ Building frontend..."
          npm run build
          
          echo "📤 Deploying to web server..."
          sudo cp -r dist/* /var/www/aaip-test/ || echo "⚠️ Manual copy required"
          
          echo "✅ Frontend deployment complete"
        ENDSSH
        
    - name: 🔄 Update Scraper via Cloudflare Tunnel
      run: |
        ssh cf-tunnel << 'ENDSSH'
          set -e
          cd '${{ secrets.TEST_DEPLOY_PATH }}/scraper'
          
          echo "🔧 Updating scraper dependencies..."
          pip3 install -r requirements.txt --quiet
          
          echo "✅ Scraper update complete"
        ENDSSH
        
    - name: 💚 Health Check
      env:
        TEST_BACKEND_URL: ${{ secrets.TEST_BACKEND_URL }}
        TEST_FRONTEND_URL: ${{ secrets.TEST_FRONTEND_URL }}
      run: |
        echo "⏳ Waiting for services to start..."
        sleep 15
        
        echo "🔍 Checking backend health..."
        if curl -f -s ${TEST_BACKEND_URL} > /dev/null; then
          echo "✅ Backend is healthy"
        else
          echo "❌ Backend health check failed"
          exit 1
        fi
        
        echo "🔍 Checking frontend..."
        if curl -f -s ${TEST_FRONTEND_URL} > /dev/null; then
          echo "✅ Frontend is accessible"
        else
          echo "❌ Frontend health check failed"
          exit 1
        fi
        
        echo "✨ All health checks passed!"
        
    - name: 📊 Deployment Summary
      if: success()
      run: |
        echo "🎉 Deployment to test server successful!"
        echo ""
        echo "📍 Endpoints:"
        echo "   Backend: ${{ secrets.TEST_BACKEND_URL }}"
        echo "   Frontend: ${{ secrets.TEST_FRONTEND_URL }}"
        echo ""
        echo "🕐 Deployed at: $(date -u)"
        
    - name: ❌ Deployment Failed
      if: failure()
      run: |
        echo "💥 Deployment failed - check logs above"
        echo "🔧 You may need to deploy manually"
